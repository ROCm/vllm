# Custom self-hosted runner labels (the fork's Strix Halo machines, plus
# upstream's autoscaling vllm-runners pool) so actionlint doesn't flag them as
# unknown in `runs-on`.
self-hosted-runner:
  labels:
    - linux-strix-halo-gpu-rocm-noneng
    - linux-strix-halo-gpu-rocm-oem
    - vllm-runners
    # Not yet in actionlint's known-label set.
    - macos-26
